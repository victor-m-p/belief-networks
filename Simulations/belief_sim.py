import numpy as np 
import pandas as pd 

# okay let us set up the basic (personal) model.
beliefs = np.array([1, -1, 1])
importance = np.array([0.8, 0.5, 0.4])
couplings = np.array([-1, 1, 0])

# okay let us think now. 
def H(): # potential personal dissonance
    pass 
